name: Opened PR Request

on:
  pull_request:
    types: opened

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: See what inside the file
      run: cat testfile.txt
